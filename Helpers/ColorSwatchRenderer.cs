// ============================================================
// QuickLook.Plugin.DevPowerTool — Helpers/ColorSwatchRenderer.cs
// No longer used — swatches are now rendered inline in the
// RichTextBox FlowDocument via InlineUIContainer.
// Kept as empty stub so the build doesn't break.
// ============================================================

namespace QuickLook.Plugin.DevPowerTool
{
    // Stub — previously used with AvalonEdit, now replaced by
    // inline InlineUIContainer swatches in PreviewPanel.RenderText()
}